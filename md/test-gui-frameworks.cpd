'Prueba de Frameworks GUI - Todos los ejemplos ejecutables

"Este archivo prueba que #avalonia, #qt y #gtk funcionan correctamente."

'=== 1. Avalonia UI (C# - Windows/Linux/macOS) ===

@{avalonia}
using System;

class Program
{
    static void Main()
    {
        Console.WriteLine("=== Avalonia UI Framework ===");
        Console.WriteLine("âœ“ Cross-platform: Windows, Linux, macOS");
        Console.WriteLine("âœ“ Usa XAML como WPF");
        Console.WriteLine("âœ“ Perfecto para portar Calcpad a Linux");
        Console.WriteLine();
        Console.WriteLine("Ejemplo de cÃ³digo:");
        Console.WriteLine("  Window window = new Window();");
        Console.WriteLine("  window.Title = \"Calcpad Avalonia\";");
        Console.WriteLine("  window.Show();");
    }
}
@{end avalonia}

'=== 2. Qt Framework (C++ - multiplataforma) ===

@{qt}
#include <iostream>
using namespace std;

int main()
{
    cout << "=== Qt GUI Framework ===" << endl;
    cout << "âœ“ Cross-platform: Windows, Linux, macOS, Android, iOS" << endl;
    cout << "âœ“ El mÃ¡s usado en la industria" << endl;
    cout << "âœ“ Usado por: VLC, OBS, Telegram, Autodesk" << endl;
    cout << endl;
    cout << "Ejemplo de cÃ³digo:" << endl;
    cout << "  QApplication app(argc, argv);" << endl;
    cout << "  QMainWindow window;" << endl;
    cout << "  window.show();" << endl;

    return 0;
}
@{end qt}

'=== 3. GTK Framework (C - nativo Linux) ===

@{gtk}
#include <stdio.h>

int main()
{
    printf("=== GTK GUI Framework ===\n");
    printf("âœ“ Nativo en Linux/GNOME\n");
    printf("âœ“ Ligero y eficiente\n");
    printf("âœ“ Usado por: GIMP, Inkscape, Blender\n");
    printf("\n");
    printf("Ejemplo de cÃ³digo:\n");
    printf("  GtkWidget *window = gtk_window_new();\n");
    printf("  gtk_window_set_title(window, \"Calcpad GTK\");\n");
    printf("  gtk_widget_show(window);\n");

    return 0;
}
@{end gtk}

'RESUMEN:

- Avalonia: Mejor opciÃ³n para portar Calcpad (C# + XAML)
- Qt: OpciÃ³n sÃ³lida con C++ (mÃ¡s complejo)
- GTK: Nativo Linux (solo C, mÃ¡s bÃ¡sico)

Para Calcpad, RECOMENDACIÃ“N: Usar Avalonia
- Puedes reutilizar casi todo el cÃ³digo WPF
- Solo cambias: System.Windows â†’ Avalonia
- El XAML es prÃ¡cticamente idÃ©ntico
